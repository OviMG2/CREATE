import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // sau alt nume al componentei principale

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
